import React from "react";
import useScrollToTop from '../components/useScrollToTop';


const Cart = () => {
    useScrollToTop();
    return (
        <main>
            <h1>Cart</h1>
            <p>This is cart page</p>
        </main>
    );
};

export default Cart;
